<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Zztomatozz" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#10b981">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%# 注意：运行 bin/rails tailwind:install 后会自动插入下面这行： %>
    <%# <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-gradient-to-b from-emerald-50 to-white text-slate-700 antialiased">
    <header class="sticky top-0 z-10 border-b border-emerald-100 bg-white/80 backdrop-blur">
      <div class="mx-auto max-w-5xl px-4 py-3 flex items-center justify-between">
        <div class="flex items-center gap-3">
          <span class="text-2xl">🍅</span>
          <span class="font-semibold text-emerald-600"><%= t("site.name") %></span>
        </div>
        <div class="flex items-center gap-4 text-sm">
          <a href="<%= pomodoro_sessions_path %>" class="px-3 py-1 rounded-lg bg-emerald-50 text-emerald-700"><%= t("nav.records") %></a>
          <% if current_user %>
            <span class="text-emerald-700"><%= current_user.email %></span>
            <%= button_to t("nav.logout"), session_path, method: :delete, class: "px-3 py-1 rounded-lg bg-emerald-100 text-emerald-700" %>
          <% else %>
            <a href="<%= new_session_path %>" class="px-3 py-1 rounded-lg bg-emerald-100 text-emerald-700"><%= t("nav.login") %></a>
            <a href="<%= new_user_path %>" class="px-3 py-1 rounded-lg bg-emerald-100 text-emerald-700"><%= t("nav.signup") %></a>
          <% end %>
          <a href="/?locale=en" class="px-3 py-1 rounded-lg bg-emerald-50 text-emerald-700"><%= t("nav.lang_en") %></a>
          <a href="/?locale=zh" class="px-3 py-1 rounded-lg bg-emerald-50 text-emerald-700"><%= t("nav.lang_zh") %></a>
        </div>
      </div>
    </header>

    <main class="mx-auto max-w-5xl px-4 py-8">
      <% if flash[:notice] %>
        <div class="mb-4 rounded-lg bg-emerald-50 text-emerald-700 p-3 text-sm"><%= flash[:notice] %></div>
      <% end %>
      <% if flash[:alert] %>
        <div class="mb-4 rounded-lg bg-red-50 text-red-700 p-3 text-sm"><%= flash[:alert] %></div>
      <% end %>
      <div class="rounded-2xl bg-white/80 backdrop-blur shadow-sm ring-1 ring-emerald-100">
        <div class="p-6">
          <%= yield %>
        </div>
      </div>
    </main>

    <footer class="mx-auto max-w-5xl px-4 pb-8">
      <div class="text-center text-xs text-slate-400">
        <span>© <%= Time.current.year %> Zztomatozz · 保持专注，提升效率</span>
      </div>
    </footer>
  </body>
</html>
